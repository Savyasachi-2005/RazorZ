# Sent as X-API-Key on every backend call. Leave empty when the backend has
# RAZORZ_API_KEYS unset (development). A browser bundle cannot keep a secret —
# use a UI-scoped key here, never an admin credential.
VITE_API_KEY=
